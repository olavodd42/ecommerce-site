import { register, login, getUser, getAllUsers, updateUser, deleteUser } from './authControllers';
import { Request, Response } from 'express';

test('should register a user', async () => {
  const req = {} as Request;
  const res = {} as Response;
  // Adicione suas asserções e mocks aqui
});